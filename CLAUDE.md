# beadstui

Project conventions and session workflow live in `AGENTS.md`.
Claude Code auto-loads it via the import below. Active planning lives in
beads (`bd list` / `bd ready`) + `docs/plans/`; `docs/adr/` holds decision
records (ADR-002, the old stabilize-and-ship spine, was retired 2026-07-14).

@AGENTS.md
