project_name: bt
builds:
  - main: ./cmd/bt
    binary: bt
    ldflags:
      - -s -w
      - -X github.com/seanmartinsmith/beadstui/pkg/version.version=v{{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ .Tag }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'

# Homebrew tap publishing
brews:
  - name: bt
    repository:
      owner: seanmartinsmith
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: https://github.com/seanmartinsmith/beadstui
    description: "Graph-aware task management TUI for beads projects"
    license: "MIT"
    install: |
      bin.install "bt"
    test: |
      system "#{bin}/bt", "--version"
    caveats: |
      To get started with bt:
        1. Initialize beads in your project: bd init
        2. Launch the TUI: bt

      For AI agent integration, use --robot-* flags:
        bt --robot-triage     # Get prioritized recommendations
        bt --robot-next       # Get single next action

# Scoop bucket publishing
scoops:
  - name: bt
    repository:
      owner: seanmartinsmith
      name: scoop-bucket
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: https://github.com/seanmartinsmith/beadstui
    description: "Graph-aware task management TUI for beads projects"
    license: MIT
