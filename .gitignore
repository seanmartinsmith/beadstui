beads_reference/
/bt
/main
*.test
cmd/bt/bt
.DS_Store

*.exe

# Build artifacts
dist/
a.out
coverage.out
cover*.out
*.cov
coverage.html
coverage_report.txt
bt_profile

# Benchmark outputs
benchmarks/baseline.txt
benchmarks/current.txt

# Local BT state and reports
.bt/
beads_report_beadstui_*.md

# Nix build artifacts
result
result-*

# Rust/WASM build artifacts
bt-graph-wasm/target/
bt-graph-wasm/pkg/
pkg/export/wasm_scorer/target/
pkg/export/wasm_scorer/pkg/
node_modules/
beadstui_graph*.html
test-results/

# Nested beads state
pkg/ui/.beads/

# Large benchmark test data (generated)
tests/testdata/benchmark/
*.prof

# E2E test logs
test_results_e2e_*.log

# Ephemeral/temporary files (agent workflow artifacts)
RESEARCH_FINDINGS.md
TOON_INTEGRATION_BRIEF.md
cov_*.out
rustc-ice-*.txt
*.snap.new
sweep_*.png
test_screenshot.png
a.out
target-*/
.playwright-mcp/

# Crush tool logs (ephemeral)
.crush/

# Legacy bv config (pre-rename) - .bt/ is the active pattern above
.bv/

# Dolt database files (added by bd init)
.dolt/
*.db
_tmp/

# Beads / Dolt files (added by bd init)
.beads-credential-key

# JSONL export is redundant with Dolt sync (bt-jlp); disabled via export.auto=false
.beads/issues.jsonl

# Multi-paragraph bead description staging — passed to `bd create --description-file`
# to avoid shell-escaping issues. Ephemeral per-session scratch; never committed.
.beads/tmp/

# Security audit working files
security-audit-results.tsv

# Git worktrees (Claude Code agent isolation)
.claude/worktrees/

# Claude Code runtime artifacts
.claude/scheduled_tasks.lock

# Superpowers brainstorming mockups (visual companion session files)
.superpowers/
